language: ruby

install:
    - bundle install
before_script:
    - rm -rf public
script:
    - bundle exec jekyll build
    - mv public/* .

deploy:
    provider: pages
    skip_cleanup: true
    github_token: $GITHUB_TOKEN
    keep_history: true
    on:
        branch: master
